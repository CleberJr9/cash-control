import 'package:flutter/material.dart';

abstract class AppColors {
  AppColors._();

  // Brand
  static const primary = Color(0xFF1F8F5F);
  static const primaryLight = Color(0xFFE3F1E9);

  // Surface
  static const background = Color(0xFFFAFAF8);
  static const canvas = Color(0xFFE7E8E4);
  static const surface = Color(0xFFFFFFFF);

  // Borders
  static const border = Color(0xFFECEDE9);

  // States
  static const disabled = Color(0xFFE3E5E1);
  static const segment = Color(0xFFEFF0ED);

  // Text
  static const textPrimary = Color(0xFF1A211D);
  static const textSecondary = Color(0xFF7A837D);
  static const textMuted = Color(0xFF9AA39E);
  static const textbutton = Color(0xFFFFFFFF);

  // Accent
  static const accentBlue = Color(0xFF2A6FDB);
  static const accentPurple = Color(0xFF6D4AD9);
  static const accentOrange = Color(0xFFE0892F);
}
